const state={menu:null,config:null,cart:new Map(),query:""};
const el=id=>document.getElementById(id);
const money=v=>new Intl.NumberFormat('tr-TR',{minimumFractionDigits:0,maximumFractionDigits:2}).format(v)+' '+(state.config?.currency||'₺');
function tableFromUrl(){const path=decodeURIComponent(location.pathname);const m=path.match(/\/masa\/(\d+)/i);if(m)return m[1];const q=new URLSearchParams(location.search);return q.get('masa')||q.get('table');}
function normalize(s){return (s||'').toLocaleLowerCase('tr-TR').normalize('NFD').replace(/[\u0300-\u036f]/g,'');}
async function boot(){try{const [m,c]=await Promise.all([fetch('/data/menu.json').then(r=>r.json()),fetch('/data/config.json').then(r=>r.json())]);state.menu=m;state.config=c;el('tagline').textContent=c.tagline||'';const table=tableFromUrl();if(table){el('tableBadge').hidden=false;el('tableBadge').textContent=`Masa ${table}`;}el('cartButton').hidden=!c.orderingEnabled;el('noteWrap').hidden=!c.allowNotes;renderNav();renderMenu();bind();}catch(e){console.error(e);el('menuRoot').innerHTML='<div class="empty">Menü şu anda yüklenemedi. Lütfen tekrar deneyin.</div>';}}
function renderNav(){const nav=el('categoryNav');nav.innerHTML='';state.menu.categories.slice().sort((a,b)=>a.sortOrder-b.sortOrder).forEach((c,i)=>{const b=document.createElement('button');b.className='category-chip'+(i===0?' active':'');b.textContent=c.name;b.onclick=()=>document.getElementById('cat-'+c.sortOrder)?.scrollIntoView({behavior:'smooth',block:'start'});nav.appendChild(b);});}
function renderMenu(){const q=normalize(state.query);const root=el('menuRoot');const html=[];for(const c of state.menu.categories.slice().sort((a,b)=>a.sortOrder-b.sortOrder)){const products=c.products.filter(p=>!q||normalize(p.name+' '+(p.description||'')).includes(q)).sort((a,b)=>a.sortOrder-b.sortOrder);if(!products.length)continue;html.push(`<section class="category-section" id="cat-${c.sortOrder}"><div class="category-title"><h2>${esc(c.name)}</h2><span>${products.length} ürün</span></div><div class="product-grid">${products.map(card).join('')}</div></section>`)}root.innerHTML=html.join('')||'<div class="empty">Aramanıza uygun ürün bulunamadı.</div>';root.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>add(b.dataset.add));}
function card(p){const image=state.config.showImages&&p.imagePath?`<div class="product-image-wrap"><img class="product-image" src="${p.imagePath}" alt="${esc(p.name)}" loading="lazy" onerror="this.parentElement.style.display='none'"></div>`:'';const add=state.config.orderingEnabled?`<button class="add-button" data-add="${p.externalId}" aria-label="${esc(p.name)} sepete ekle">+</button>`:'';return `<article class="product-card">${image}<div class="product-content"><h3 class="product-name">${esc(p.name)}</h3>${p.description?`<p class="product-desc">${esc(p.description)}</p>`:''}<div class="product-bottom"><span class="price">${money(p.price)}</span>${add}</div></div></article>`}
function allProducts(){return state.menu.categories.flatMap(c=>c.products)}
function add(id){const p=allProducts().find(x=>String(x.externalId)===String(id));if(!p)return;const old=state.cart.get(id)||{product:p,qty:0};old.qty++;state.cart.set(id,old);updateCart();toast(`${p.name} sepete eklendi`)}
function change(id,delta){const row=state.cart.get(id);if(!row)return;row.qty+=delta;if(row.qty<=0)state.cart.delete(id);else state.cart.set(id,row);updateCart();renderCart()}
function updateCart(){let count=0;for(const x of state.cart.values())count+=x.qty;el('cartCount').textContent=count;}
function renderCart(){const rows=[...state.cart.entries()];el('cartItems').innerHTML=rows.length?rows.map(([id,x])=>`<div class="cart-row"><div><h3>${esc(x.product.name)}</h3><small>${money(x.product.price)} × ${x.qty}</small></div><div class="qty"><button data-q="${id}" data-d="-1">−</button><strong>${x.qty}</strong><button data-q="${id}" data-d="1">+</button></div></div>`).join(''):'<div class="empty">Sepetiniz boş.</div>';el('cartTotal').textContent=money(rows.reduce((s,[,x])=>s+x.product.price*x.qty,0));el('cartItems').querySelectorAll('[data-q]').forEach(b=>b.onclick=()=>change(b.dataset.q,Number(b.dataset.d)));}
function openCart(){renderCart();el('cartSheet').hidden=false;document.body.style.overflow='hidden'}function closeCart(){el('cartSheet').hidden=true;document.body.style.overflow=''}
async function submit(){if(!state.cart.size)return toast('Sepetiniz boş.');const table=tableFromUrl();if(!table)return toast('Sipariş için masa QR kodunu okutmalısınız.');const payload={table:Number(table),note:el('orderNote').value.trim(),items:[...state.cart.values()].map(x=>({externalId:x.product.externalId,quantity:x.qty,unitPrice:x.product.price}))};try{const r=await fetch('/api/orders',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const d=await r.json();if(!r.ok)throw new Error(d.message||'Sipariş gönderilemedi');state.cart.clear();updateCart();closeCart();toast('Siparişiniz alındı.');}catch(e){toast(e.message||'Sipariş gönderilemedi.');}}
function bind(){el('searchInput').addEventListener('input',e=>{state.query=e.target.value;renderMenu()});el('cartButton').onclick=openCart;document.querySelectorAll('[data-close-cart]').forEach(b=>b.onclick=closeCart);el('submitOrder').onclick=submit;}
function toast(t){const x=el('toast');x.textContent=t;x.hidden=false;clearTimeout(window.__toast);window.__toast=setTimeout(()=>x.hidden=true,2600)}
function esc(s){return String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
document.addEventListener('DOMContentLoaded',boot);
