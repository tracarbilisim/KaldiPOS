<Page x:Class="KaldiPOS.Views.OrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#11100E">

    <Page.Resources>
        <Style x:Key="CategoryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="48"/>
            <Setter Property="MinWidth" Value="118"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Foreground" Value="#FFFDF8"/>
            <Setter Property="Background" Value="#211E1A"/>
            <Setter Property="BorderBrush" Value="#765A32"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="9">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#30291F"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#D0A354"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#D0A354"/>
                                <Setter Property="Foreground" Value="#18140E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="52"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.88"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.72"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="330"/>
        </Grid.ColumnDefinitions>

        <!-- ÜRÜN ÇALIŞMA ALANI -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="68"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- YATAY KATEGORİLER VE ARAMA -->
            <Border Grid.Row="0"
                    Padding="10"
                    Background="#171512"
                    BorderBrush="#765A32"
                    BorderThickness="1"
                    CornerRadius="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="230"/>
                    </Grid.ColumnDefinitions>

                    <ScrollViewer Grid.Column="0"
                                  HorizontalScrollBarVisibility="Hidden"
                                  VerticalScrollBarVisibility="Disabled">
                        <ItemsControl x:Name="CategoriesItemsControl">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding}"
                                            Tag="{Binding}"
                                            Style="{StaticResource CategoryButtonStyle}"
                                            Click="CategoryButton_Click"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <Border Grid.Column="1"
                            Height="46"
                            Margin="8,0,0,0"
                            VerticalAlignment="Center"
                            Background="#211E1A"
                            BorderBrush="#765A32"
                            BorderThickness="1"
                            CornerRadius="9">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="ProductSearchTextBox"
                                     Margin="12,0,4,0"
                                     VerticalContentAlignment="Center"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="#FFFDF8"
                                     CaretBrush="#FFFDF8"
                                     FontSize="14"
                                     ToolTip="Ürün ara"
                                     TextChanged="ProductSearchTextBox_TextChanged"/>
                            <TextBlock Grid.Column="1"
                                       Text="⌕"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       FontSize="23"
                                       Foreground="#D0A354"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- ÜRÜNLER -->
            <Border Grid.Row="2"
                    Padding="12"
                    Background="#171512"
                    BorderBrush="#765A32"
                    BorderThickness="1"
                    CornerRadius="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="38"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="CategoryTitleText"
                               Text="Tüm Ürünler"
                               Margin="4,0,0,4"
                               VerticalAlignment="Center"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="#FFFDF8"/>

                    <ScrollViewer Grid.Row="1"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl x:Name="ProductsItemsControl">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Width="150"
                                            Height="156"
                                            Margin="5"
                                            Padding="0"
                                            Tag="{Binding}"
                                            Background="#211E1A"
                                            BorderBrush="#765A32"
                                            BorderThickness="1"
                                            Cursor="Hand"
                                            Click="ProductButton_Click">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="Card"
                                                        Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="10"
                                                        RenderTransformOrigin="0.5,0.5">
                                                    <Border.RenderTransform>
                                                        <ScaleTransform x:Name="CardScale"
                                                                        ScaleX="1"
                                                                        ScaleY="1"/>
                                                    </Border.RenderTransform>
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="94"/>
                                                            <RowDefinition Height="*"/>
                                                        </Grid.RowDefinitions>

                                                        <Border Margin="5"
                                                                Background="#12110F"
                                                                CornerRadius="7"
                                                                ClipToBounds="True">
                                                            <Image Source="{Binding ImagePath}"
                                                                   Stretch="UniformToFill"
                                                                   RenderOptions.BitmapScalingMode="LowQuality"/>
                                                        </Border>

                                                        <Grid Grid.Row="1" Margin="7,1,7,7">
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="*"/>
                                                                <RowDefinition Height="Auto"/>
                                                            </Grid.RowDefinitions>
                                                            <TextBlock Text="{Binding Name}"
                                                                       HorizontalAlignment="Center"
                                                                       VerticalAlignment="Center"
                                                                       TextAlignment="Center"
                                                                       TextWrapping="Wrap"
                                                                       FontSize="13.5"
                                                                       FontWeight="SemiBold"
                                                                       Foreground="#FFFDF8"/>
                                                            <TextBlock Grid.Row="1"
                                                                       Text="{Binding PriceText}"
                                                                       Margin="0,2,0,0"
                                                                       HorizontalAlignment="Center"
                                                                       FontSize="15"
                                                                       FontWeight="Bold"
                                                                       Foreground="#E2B85F"/>
                                                        </Grid>
                                                    </Grid>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Card" Property="BorderBrush" Value="#D0A354"/>
                                                        <Setter TargetName="Card" Property="Background" Value="#2B261E"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="Card" Property="Opacity" Value="0.88"/>
                                                        <Setter TargetName="Card" Property="Background" Value="#3A3022"/>
                                                        <Setter TargetName="Card" Property="BorderBrush" Value="#E2B85F"/>
                                                        <Setter TargetName="Card"
                                                                Property="RenderTransform">
                                                            <Setter.Value>
                                                                <ScaleTransform ScaleX="0.96"
                                                                ScaleY="0.96"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- ADİSYON -->
        <Border Grid.Column="2"
                Padding="14"
                Background="#171512"
                BorderBrush="#765A32"
                BorderThickness="1"
                CornerRadius="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="54"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="1"/>
                    <RowDefinition Height="70"/>
                    <RowDefinition Height="52"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="48"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="82"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Adisyon"
                                   FontSize="19"
                                   FontWeight="Bold"
                                   Foreground="#FFFDF8"/>
                        <TextBlock x:Name="OrderCountText"
                                   Text="0 ürün"
                                   Margin="0,2,0,0"
                                   FontSize="12"
                                   Foreground="#BDB7AD"/>
                    </StackPanel>
                    <Button Grid.Column="1"
                            Content="Temizle"
                            Height="38"
                            Background="#D96267"
                            BorderBrush="#EA777C"
                            Foreground="White"
                            FontWeight="SemiBold"
                            Style="{StaticResource ActionButtonStyle}"
                            Click="ClearOrderButton_Click"/>
                </Grid>

                <ListBox x:Name="OrderListBox"
                         Grid.Row="1"
                         Margin="0,8,0,10"
                         ItemsSource="{Binding OrderItems}"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,0,0,7"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Focusable" Value="False"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="8"
                                    BorderThickness="1"
                                    CornerRadius="8">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#211E1A"/>
                                        <Setter Property="BorderBrush" Value="#4A3B25"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasUnsentItems}" Value="True">
                                                <Setter Property="Background" Value="#332A1B"/>
                                                <Setter Property="BorderBrush" Value="#E2B85F"/>
                                                <Setter Property="BorderThickness" Value="2"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="72"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="−"
                                                Width="30"
                                                Height="30"
                                                Padding="0"
                                                Tag="{Binding}"
                                                Style="{StaticResource CategoryButtonStyle}"
                                                MinWidth="30"
                                                Margin="0"
                                                Click="DecreaseButton_Click"/>
                                        <TextBlock Text="{Binding Quantity}"
                                                   Width="30"
                                                   VerticalAlignment="Center"
                                                   TextAlignment="Center"
                                                   FontSize="14"
                                                   FontWeight="Bold"
                                                   Foreground="#FFFDF8"/>
                                        <Button Content="+"
                                                Width="30"
                                                Height="30"
                                                Padding="0"
                                                Tag="{Binding}"
                                                Style="{StaticResource CategoryButtonStyle}"
                                                MinWidth="30"
                                                Margin="0"
                                                Click="IncreaseButton_Click"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Name}"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       Foreground="#FFFDF8"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <Border Grid.Column="1"
                                                    Margin="8,0,0,0"
                                                    Padding="7,3"
                                                    CornerRadius="5">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#31543B"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding HasUnsentItems}" Value="True">
                                                                <Setter Property="Background" Value="#8A6420"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding StatusText}"
                                                           FontSize="10"
                                                           FontWeight="Bold"
                                                           Foreground="White"/>
                                            </Border>
                                        </Grid>
                                        <TextBlock Text="{Binding LineTotalText}"
                                                   Margin="0,3,0,0"
                                                   FontSize="12"
                                                   Foreground="#E2B85F"/>
                                        <Grid Margin="0,5,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding NoteDisplay}"
                                                       VerticalAlignment="Center"
                                                       FontSize="11"
                                                       FontStyle="Italic"
                                                       Foreground="#BDB7AD"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <Button Grid.Column="1"
                                                    Content="Not"
                                                    Width="48"
                                                    Height="28"
                                                    Margin="8,0,0,0"
                                                    Padding="0"
                                                    Tag="{Binding}"
                                                    Style="{StaticResource CategoryButtonStyle}"
                                                    Click="NoteButton_Click"/>
                                        </Grid>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Border Grid.Row="2" Background="#604A29"/>

                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="TOPLAM"
                               VerticalAlignment="Center"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="#FFFDF8"/>
                    <TextBlock x:Name="TotalText"
                               Grid.Column="1"
                               Text="0,00 ₺"
                               VerticalAlignment="Center"
                               FontSize="25"
                               FontWeight="Bold"
                               Foreground="#58C878"/>
                </Grid>

                <Button Grid.Row="4"
                        Content="Siparişi Gönder"
                        Background="#D0A354"
                        BorderBrush="#E2B85F"
                        Foreground="#18140E"
                        Style="{StaticResource ActionButtonStyle}"
                        Click="SendOrderButton_Click"/>

                <Button Grid.Row="6"
                        Content="Ödeme Al"
                        Background="#211E1A"
                        BorderBrush="#765A32"
                        Foreground="#FFFDF8"
                        Style="{StaticResource ActionButtonStyle}"
                        Click="PaymentButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</Page>
