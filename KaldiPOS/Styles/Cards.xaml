<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="Card.Base" TargetType="Border">
        <Setter Property="Background" Value="#24211D" />
        <Setter Property="BorderBrush" Value="#765A32" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="14" />
        <Setter Property="Padding" Value="20" />
        <Setter Property="SnapsToDevicePixels" Value="True" />

        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="24"
                                  ShadowDepth="0"
                                  Opacity="0.32"
                                  Color="#000000" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Card.Glass"
           TargetType="Border"
           BasedOn="{StaticResource Card.Base}">
        <Setter Property="Background" Value="#E61A1815" />
        <Setter Property="BorderBrush" Value="#765A32" />
    </Style>

    <Style x:Key="Card.Panel"
           TargetType="Border"
           BasedOn="{StaticResource Card.Base}">
        <Setter Property="Background" Value="#1A1815" />
        <Setter Property="BorderBrush" Value="#765A32" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Padding" Value="16" />
    </Style>

    <Style x:Key="Card.Status"
           TargetType="Border"
           BasedOn="{StaticResource Card.Base}">
        <Setter Property="Background" Value="#11100E" />
        <Setter Property="BorderBrush" Value="#765A32" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Padding" Value="12,8" />
    </Style>

</ResourceDictionary>